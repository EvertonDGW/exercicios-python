"""
For + Range
range -> range(start, stop, step)
"""
numeros = range(0, 100, 8)   # 8 significa que ele vai saltar de 8 em 8 ate chegar em 100 ou aproximado de 100

for numero in numeros: # for signifia ( para ).ele faz com que os valores dentro da variavel numeros sejam guardados dentro da varial numero, para cada iteração
    print(numero)

#imagine uma contagem de 0 a 10, o loop for vai analisar cada numero, isso e a iteração
    #lembre-se interações occorem nos loop for ou while